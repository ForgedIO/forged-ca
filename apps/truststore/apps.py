from django.apps import AppConfig


class TrustStoreConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.truststore"
    verbose_name = "Trust-store Distribution"
